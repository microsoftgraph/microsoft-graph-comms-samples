namespace RecordingBot.Model.Constants
{
    public static class BotConstants
    {
        public const uint NUMBER_OF_MULTIVIEW_SOCKETS = 3;
        public const string DEFAULT_OUTPUT_FOLDER = "teams-recording-bot";
        public const string TOPIC_ENDPOINT = "https://{0}.{1}-1.eventgrid.azure.net/api/events";
    }
}
